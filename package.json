{
  "name": "daylens",
  "version": "1.0.0",
  "private": true,
  "description": "Privacy-first personal analytics dashboard for understanding what shapes your days.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/main.js && node --check src/store.js && node --check src/analytics.js && node --check src/cloud.js && node --check src/utils.js",
    "ci": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module"
}
